// player state. Currently only used for disabling input if fullscreen map is shown. Feel free to add other states as needed.
public enum PlayerState
{
    Normal,
    DisableInput,
    Attacking, 
}
